version: "3.6"

networks:
  net_apartment:

services:
  svc_apartment:
    image: revaturecloud/servicehub-apartment:edge
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5
        window: 60s
